#ifndef DISCOS_HPP
#define DiSCOS_HPP

#include <string>
#include <vector>

std::vector<std::string> listarDiscos(const std :: string &programaPath);

//std::vector indica que a função retorna um objeto do tipo vector, um conteiner que armazena uma sequencia de elementos.
//<std::string> Especifica que os elementos armazenados no vetor são do tipo string. 
// listarDiscos nome da função 
// (const std :: string &programaPath) essa função recebe um único parametro, prgoramaPath 


#endif 

